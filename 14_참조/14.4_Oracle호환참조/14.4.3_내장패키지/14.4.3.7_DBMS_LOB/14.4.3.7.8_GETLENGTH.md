# GETLENGTH — EPAS 18 내장 패키지 DBMS_LOB
> 원문: [EPAS v18 — Reference › Oracle compatibility › Built-in packages › DBMS_LOB › GETLENGTH](https://www.enterprisedb.com/docs/epas/latest/reference/oracle_compatibility_reference/epas_compat_bip_guide/03_built-in_packages/06_dbms_lob/08_getlength/)
> EPAS v18 공식 문서의 한글 **전문 번역**입니다. 패키지·프로시저·함수 이름은 원문 그대로 보존했습니다.

# GETLENGTH

`GETLENGTH` 함수는 대형 객체(large object)의 길이를 반환합니다.

```sql
<amount> NUMBER GETLENGTH(<lob_loc> BLOB)

<amount> NUMBER GETLENGTH(<lob_loc> CLOB)

<amount> NUMBER GETLENGTH(<file_loc> IN BFILE)
```

## 파라미터 (Parameters)

`amount`

대형 객체의 길이(바이트 또는 문자 단위)입니다.

`file_loc`

길이를 구할 BFILE에 대한 파일 로케이터(locator)입니다.

`lob_loc`

길이를 구할 대형 객체의 대형 객체 로케이터(locator)입니다.

## 예제 (Example)

```sql
DECLARE
   File_loc      BFILE = BFILENAME('ANOTHER_DIR', 'a.txt');
   Length        NUMBER;
BEGIN
   DBMS_LOB.OPEN(File_loc, DBMS_LOB.LOB_READONLY);
   /* Get the length of the LOB: */
   Length := DBMS_LOB.GETLENGTH(File_loc);
   IF Length IS NULL THEN
       DBMS_OUTPUT.PUT_LINE('BFILE is null.');
   ELSE
       DBMS_OUTPUT.PUT_LINE('The length is ' || length);
   END IF;
   /* Close the BFILE: */
   DBMS_LOB.CLOSE(File_loc);
END;
```
