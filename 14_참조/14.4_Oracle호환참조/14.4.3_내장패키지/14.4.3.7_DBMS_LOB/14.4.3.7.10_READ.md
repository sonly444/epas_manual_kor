# READ — EPAS 18 내장 패키지 DBMS_LOB
> 원문: [EPAS v18 — Reference › Oracle compatibility › Built-in packages › DBMS_LOB › READ](https://www.enterprisedb.com/docs/epas/latest/reference/oracle_compatibility_reference/epas_compat_bip_guide/03_built-in_packages/06_dbms_lob/10_read/)
> EPAS v18 공식 문서의 한글 **전문 번역**입니다. 패키지·프로시저·함수 이름은 원문 그대로 보존했습니다.

# READ

`READ` 프로시저는 지정된 오프셋에서 시작하여 대형 객체(large object)의 일부를 버퍼로 읽어 들입니다.

```sql
READ(<lob_loc> { BLOB | CLOB }, <amount> IN OUT NUMBER,
  <offset> NUMBER, <buffer> OUT { RAW | VARCHAR2 })

READ(<file_loc> { BFILE }, <amount> IN OUT NUMBER,
  <offset> IN NUMBER, <buffer> OUT { RAW })
```

## 파라미터 (Parameters)

`lob_loc`

읽을 대형 객체의 대형 객체 로케이터(locator)입니다.

`file_loc`

읽을 BFILE의 파일 로케이터(locator)입니다.

`amount`

읽은 바이트/문자 수입니다. 더 이상 읽을 데이터가 없으면 `amount`는 `0`을 반환하고 `DATA_NOT_FOUND` 예외가 발생합니다.

`offset`

읽기를 시작할 위치입니다. 첫 번째 바이트/문자의 위치는 1입니다.

`buffer`

대형 객체를 받을 변수입니다. `lob_loc`가 `BLOB`이면 `buffer`는 `RAW`여야 합니다. `lob_loc`가 `CLOB`이면 `buffer`는 `VARCHAR2`여야 합니다. `file_loc`가 `BFILE`이면 `buffer`는 `RAW`여야 합니다.

## 예제 (Example)

```sql
DECLARE
   src_loc  bfile := BFILENAME('ANOTHER_DIR', 'a.txt');
   Buffer   RAW(1024);
   Amount   NUMBER := 1024;
   Position NUMBER := 1;
BEGIN
  /* Opening the source BFILE is mandatory */
  DBMS_LOB.OPEN (src_loc, DBMS_LOB.LOB_READONLY);
  DBMS_LOB.READ (src_loc, Amount, Position, Buffer);
 /* Close the BFILE: */
  DBMS_LOB.FILECLOSE(src_loc);
  COMMIT;
END;
```
