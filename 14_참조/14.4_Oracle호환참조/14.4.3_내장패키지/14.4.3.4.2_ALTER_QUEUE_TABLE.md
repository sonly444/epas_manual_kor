# ALTER_QUEUE_TABLE — EPAS 18 내장 패키지 DBMS_AQADM

> 원문: [EPAS v18 — Reference › Oracle compatibility › Built-in packages › DBMS_AQADM › ALTER_QUEUE_TABLE](https://www.enterprisedb.com/docs/epas/latest/reference/oracle_compatibility_reference/epas_compat_bip_guide/03_built-in_packages/03_dbms_aqadm/02_alter_queue_table/)
> EPAS v18 공식 문서의 한글 **전문 번역**입니다. 패키지·프로시저·함수 이름은 원문 그대로 보존했습니다.

# ALTER_QUEUE_TABLE

기존 큐 테이블을 수정하려면 `ALTER_QUEUE_TABLE` 프로시저를 사용합니다. 시그니처는 다음과 같습니다.

```sql
ALTER_QUEUE_TABLE (
  <queue_table> IN VARCHAR2,
  <comment> IN VARCHAR2 DEFAULT NULL,
  <primary_instance> IN BINARY_INTEGER DEFAULT 0,
  <secondary_instance> IN BINARY_INTEGER DEFAULT 0,
```

## 매개변수 (Parameters)

`queue_table`

큐 테이블의 (선택적으로 스키마로 한정된) 이름입니다.

`comment`

`comment` 매개변수를 사용하여 큐 테이블에 대한 주석을 제공합니다.

`primary_instance`

`primary_instance`는 호환성을 위해 허용되고 저장되지만 무시됩니다.

`secondary_instance`

`secondary_instance`는 호환성을 위해 허용되지만 무시됩니다.

## 예제 (Example)

다음 명령은 `work_order_table`이라는 이름의 큐 테이블을 수정합니다.

```sql
EXEC DBMS_AQADM.ALTER_QUEUE_TABLE
      (queue_table => 'work_order_table', comment => 'This queue table
contains work orders for the shipping department.');
```

큐 테이블의 이름은 `work_order_table`입니다. 이 명령은 큐 테이블의 정의에 주석을 추가합니다.
