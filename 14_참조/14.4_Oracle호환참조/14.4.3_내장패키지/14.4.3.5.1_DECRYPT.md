# DECRYPT — EPAS 18 내장 패키지 DBMS_CRYPTO

> 원문: [EPAS v18 — Reference › Oracle compatibility › Built-in packages › DBMS_CRYPTO › DECRYPT](https://www.enterprisedb.com/docs/epas/latest/reference/oracle_compatibility_reference/epas_compat_bip_guide/03_built-in_packages/04_dbms_crypto/01_decrypt/)
> EPAS v18 공식 문서의 한글 **전문 번역**입니다. 패키지·프로시저·함수 이름은 원문 그대로 보존했습니다.

# DECRYPT

`DECRYPT` 함수 또는 프로시저는 사용자가 지정한 암호 알고리즘, 키, 그리고 선택적 초기화 벡터를 사용하여 데이터를 복호화합니다. `DECRYPT` 함수의 시그니처는 다음과 같습니다.

```sql
DECRYPT
  (<src> IN RAW, <typ> IN INTEGER, <key> IN RAW, <iv> IN RAW
   DEFAULT NULL) RETURN RAW
```

`DECRYPT` 프로시저의 시그니처는 다음과 같습니다.

```sql
DECRYPT
  (<dst> INOUT BLOB, <src> IN BLOB, <typ> IN INTEGER, <key> IN RAW,
   <iv> IN RAW DEFAULT NULL)
```

또는

```sql
DECRYPT
  (<dst> INOUT CLOB, <src> IN CLOB, <typ> IN INTEGER, <key> IN RAW,
   <iv> IN RAW DEFAULT NULL)
```

프로시저로 호출될 때, `DECRYPT`는 `BLOB` 또는 `CLOB` 데이터를 사용자가 지정한 `BLOB`에 반환합니다.

## 매개변수 (Parameters)

`dst`

`dst`는 `DECRYPT` 프로시저의 출력이 기록될 `BLOB`의 이름을 지정합니다. `DECRYPT` 프로시저는 현재 `dst`에 존재하는 기존 데이터를 덮어씁니다.

`src`

`src`는 복호화할 소스 데이터를 지정합니다. `DECRYPT`를 함수로 호출하는 경우 `RAW` 데이터를 지정합니다. `DECRYPT`를 프로시저로 호출하는 경우 `BLOB` 또는 `CLOB` 데이터를 지정합니다.

`typ`

`typ`는 블록 암호 유형과 모든 수정자를 지정합니다. `src`가 암호화될 때 지정된 유형과 일치시켜야 합니다. EDB Postgres Advanced Server는 다음 블록 암호 알고리즘, 수정자, 암호 스위트를 지원합니다.

| 블록 암호 알고리즘 |  |
| --- | --- |
| `ENCRYPT_DES` | `CONSTANT INTEGER := 1;` |
| `ENCRYPT_3DES` | `CONSTANT INTEGER := 3;` |
| `ENCRYPT_AES` | `CONSTANT INTEGER := 4;` |
| `ENCRYPT_AES128` | `CONSTANT INTEGER := 6;` |
| `ENCRYPT_AES192` | `CONSTANT INTEGER := 192;` |
| `ENCRYPT_AES256` | `CONSTANT INTEGER := 256;` |
| **블록 암호 수정자** |  |
| `CHAIN_CBC` | `CONSTANT INTEGER := 256;` |
| `CHAIN_ECB` | `CONSTANT INTEGER := 768;` |
| **블록 암호 패딩 수정자** |  |
| `PAD_PKCS5` | `CONSTANT INTEGER := 4096;` |
| `PAD_NONE` | `CONSTANT INTEGER := 8192;` |
| **블록 암호 스위트** |  |
| `DES_CBC_PKCS5` | `CONSTANT INTEGER := ENCRYPT_DES + CHAIN_CBC + PAD_PKCS5;` |
| `DES3_CBC_PKCS5` | `CONSTANT INTEGER := ENCRYPT_3DES + CHAIN_CBC + PAD_PKCS5;` |
| `AES_CBC_PKCS5` | `CONSTANT INTEGER := ENCRYPT_AES + CHAIN_CBC + PAD_PKCS5;` |

`key`

`key`는 사용자가 정의한 복호화 키를 지정합니다. `src`가 암호화될 때 지정된 키와 일치시켜야 합니다.

`iv`

`iv`(선택 사항)는 초기화 벡터를 지정합니다. `src`가 암호화될 때 초기화 벡터가 지정되었다면, `src`를 복호화할 때도 초기화 벡터를 지정해야 합니다. 기본값은 `NULL`입니다.

## 예제 (Examples)

이 예제는 `DBMS_CRYPTO.DECRYPT` 함수를 사용하여 `passwords` 테이블에서 검색한 암호화된 비밀번호를 복호화합니다.

```sql
CREATE TABLE passwords
(
  principal VARCHAR2(90) PRIMARY KEY, -- username
  ciphertext RAW(9) -- encrypted password
);

CREATE FUNCTION get_password(username VARCHAR2) RETURN RAW AS
 typ       INTEGER := DBMS_CRYPTO.DES_CBC_PKCS5;
 key       RAW(128) := 'my secret key';
 iv        RAW(100) := 'my initialization vector';
 password  RAW(2048);
BEGIN

  SELECT ciphertext INTO password FROM passwords WHERE principal = username;

  RETURN dbms_crypto.decrypt(password, typ, key, iv);
END;
```

`DECRYPT`를 호출할 때는 대상을 암호화할 때 사용된 것과 동일한 암호 유형, 키 값, 초기화 벡터를 전달해야 합니다.
