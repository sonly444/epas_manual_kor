# ENCRYPT — EPAS 18 내장 패키지 DBMS_CRYPTO

> 원문: [EPAS v18 — Reference › Oracle compatibility › Built-in packages › DBMS_CRYPTO › ENCRYPT](https://www.enterprisedb.com/docs/epas/latest/reference/oracle_compatibility_reference/epas_compat_bip_guide/03_built-in_packages/04_dbms_crypto/02_encrypt/)
> EPAS v18 공식 문서의 한글 **전문 번역**입니다. 패키지·프로시저·함수 이름은 원문 그대로 보존했습니다.

# ENCRYPT

`ENCRYPT` 함수 또는 프로시저는 사용자가 지정한 알고리즘, 키, 그리고 선택적 초기화 벡터를 사용하여 `RAW`, `BLOB`, 또는 `CLOB` 데이터를 암호화합니다. `ENCRYPT` 함수의 시그니처는 다음과 같습니다.

```sql
ENCRYPT
  (<src> IN RAW, <typ> IN INTEGER, <key> IN RAW,
   <iv> IN RAW DEFAULT NULL) RETURN RAW
```

`ENCRYPT` 프로시저의 시그니처는 다음과 같습니다.

```sql
ENCRYPT
  (<dst> INOUT BLOB, <src> IN BLOB, <typ> IN INTEGER, <key> IN RAW,
   <iv> IN RAW DEFAULT NULL)
```

또는

```sql
ENCRYPT
  (<dst> INOUT BLOB, <src> IN CLOB, <typ> IN INTEGER, <key> IN RAW,
   <iv> IN RAW DEFAULT NULL)
```

프로시저로 호출될 때, `ENCRYPT`는 `BLOB` 또는 `CLOB` 데이터를 사용자가 지정한 `BLOB`에 반환합니다.

## 매개변수 (Parameters)

`dst`

`dst`는 `ENCRYPT` 프로시저의 출력을 기록할 `BLOB`의 이름을 지정합니다. `ENCRYPT` 프로시저는 현재 `dst`에 존재하는 기존 데이터를 덮어씁니다.

`src`

`src`는 암호화할 소스 데이터를 지정합니다. `ENCRYPT`를 함수로 호출하는 경우 `RAW` 데이터를 지정합니다. `ENCRYPT`를 프로시저로 호출하는 경우 `BLOB` 또는 `CLOB` 데이터를 지정합니다.

`typ`

`typ`는 `ENCRYPT`가 사용하는 블록 암호 유형과 모든 수정자를 지정합니다. EDB Postgres Advanced Server는 표에 표시된 블록 암호 알고리즘, 수정자, 암호 스위트를 지원합니다.

| 블록 암호 알고리즘 |  |
| --- | --- |
| `ENCRYPT_DES` | `CONSTANT INTEGER := 1;` |
| `ENCRYPT_3DES` | `CONSTANT INTEGER := 3;` |
| `ENCRYPT_AES` | `CONSTANT INTEGER := 4;` |
| `ENCRYPT_AES128` | `CONSTANT INTEGER := 6;` |
| `ENCRYPT_AES192` | `CONSTANT INTEGER := 192;` |
| `ENCRYPT_AES256` | `CONSTANT INTEGER := 256;` |
| **블록 암호 수정자** |  |
| `CHAIN_CBC` | `CONSTANT INTEGER := 256;` |
| `CHAIN_ECB` | `CONSTANT INTEGER := 768;` |
| **블록 암호 패딩 수정자** |  |
| `PAD_PKCS5` | `CONSTANT INTEGER := 4096;` |
| `PAD_NONE` | `CONSTANT INTEGER := 8192;` |
| **블록 암호 스위트** |  |
| `DES_CBC_PKCS5` | `CONSTANT INTEGER := ENCRYPT_DES + CHAIN_CBC + PAD_PKCS5;` |
| `DES3_CBC_PKCS5` | `CONSTANT INTEGER := ENCRYPT_3DES + CHAIN_CBC + PAD_PKCS5;` |
| `AES_CBC_PKCS5` | `CONSTANT INTEGER := ENCRYPT_AES + CHAIN_CBC + PAD_PKCS5;` |

`key`

`key`는 암호화 키를 지정합니다.

`iv`

`iv`(선택 사항)는 초기화 벡터를 지정합니다. 기본적으로 `iv`는 `NULL`입니다.

## 예제 (Examples)

이 예제는 `DBMS_CRYPTO.DES_CBC_PKCS5` 블록 암호 스위트(미리 정의된 알고리즘과 수정자의 집합)를 사용하여 `passwords` 테이블에서 검색한 값을 암호화합니다.

```sql
CREATE TABLE passwords
(
  principal   VARCHAR2(90) PRIMARY KEY, -- username
  ciphertext  RAW(9) -- encrypted password
);
CREATE PROCEDURE set_password(username VARCHAR2, cleartext RAW) AS
 typ         INTEGER := DBMS_CRYPTO.DES_CBC_PKCS5;
 key         RAW(128) := 'my secret key';
 iv          RAW(100) := 'my initialization vector';
 encrypted   RAW(2048);
BEGIN
  encrypted := dbms_crypto.encrypt(cleartext, typ, key, iv);
  UPDATE passwords SET ciphertext = encrypted WHERE principal = username;
END;
```

`ENCRYPT`는 비밀번호를 암호화할 때 `my secret key`라는 키 값과 `my initialization vector`라는 초기화 벡터를 사용합니다. 비밀번호를 복호화할 때는 동일한 키와 초기화 벡터를 지정하십시오.
